lint:
	@echo -e "${LIGHTPURPLE}+ make target: $@${RESET}"
	$(info skipping lint...)

pr:
	@echo -e "${LIGHTPURPLE}+ make target: $@${RESET}"
	$(info skipping pr...)

k8s:
	@echo -e "${LIGHTPURPLE}+ make target: $@${RESET}"
	$(info skipping k8s...)
